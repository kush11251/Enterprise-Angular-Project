import { Component } from '@angular/core';import { DataService } from '../services/data.service';import { IUser } from '../models/user.interface';@Component({  selector: 'app-root',  template: '<button (click)="fetchData()">Fetch Data</button>'})export class AppComponent {  users: IUser[];  constructor(private dataService: DataService) { }  fetchData(): void {    this.dataService.fetchUsers().subscribe(users => this.users = users);  }}